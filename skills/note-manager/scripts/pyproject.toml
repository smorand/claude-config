[project]
name = "note-manager"
version = "0.1.0"
description = "Google Keep notes management scripts"
requires-python = ">=3.11"
dependencies = [
    "gkeepapi>=0.14.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
